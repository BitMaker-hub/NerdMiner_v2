// Note: Use a unique include guard to avoid clashing with Arduino's <WebServer.h>
#ifndef NM_WEB_SERVER_H
#define NM_WEB_SERVER_H

void setupWebServer();
void handleWebServerClient();

#endif // NM_WEB_SERVER_H
